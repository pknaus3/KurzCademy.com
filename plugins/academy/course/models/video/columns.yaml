# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    user:
        label: Pužívaťeľ
        searchable: true
        relation: user
        select: name
    step:
        label: krok_id
        relation: step
        searchable: true
        select: step_name
    link:
        label: link na video
        searchable: true
