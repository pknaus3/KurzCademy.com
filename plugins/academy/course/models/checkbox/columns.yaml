# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    user:
        label: Pužívaťeľ
        searchable: true
        relation: user
        select: name
    step:
        label: Krok
        relation: step
        searchable: true
        select: step_name

